package library;

import library.interfaces.hardware.ICardReaderListener;
import library.panels.MainPanel;

public class CardReaderListener implements ICardReaderListener
{

	@Override
	public void cardSwiped(int cardData)
	{
		/*MainPanel mainPanel = MainPanel.getInstance(null);
		mainPanel.*/
	}

}
